<?php

	$output .= "<h2>Groups</h2>";

	//TODO Kriss - Display the Create/Join group dialogue.
	$output .= "";

	//TODO Kriss - IF there's a ?group=THING query, list the posts from that group.
				//ELSE IF - ?leave=GROUP leave the group
				//ELSE IF - ?group=GROUP&add=USERNAME ad USERNAME to GROUP
				//ELSE List off the groups that the user is a member of.
					//IF user is admin, list ALL groups

?>